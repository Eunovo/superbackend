import { GraphQLObjectType } from "graphql";
import { RepoBuilder } from "../RepoBuilder";

export const buildMongoRepo: RepoBuilder = (schema: GraphQLObjectType) => {
    throw new Error("Method not implemented");
}
