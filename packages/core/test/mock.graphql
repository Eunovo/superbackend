"""
@model
@principal
"""
type User {
    """
    @unique
    @allowOnMatch('user', 'create', 'read')
    """
    username: String!
    """
    @default('user')
    """
    role: Role!
}

enum Role {
    user
    admin
}

"""
@model
"""
type Test {
    """
    @OneToOne('User', '_id')
    """
    user: User!
}

"""
@model
"""
type Store {
    """
    @ManyToOne('User', 'username')
    """
    owner: String!
}
