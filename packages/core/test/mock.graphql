"""
@model
"""
type User {
    """
    @id
    """
    _id: ID!
    """
    @unique
    """
    username: String!
    """
    @default('user')
    """
    role: Role!
}

enum Role {
    user
    admin
}

"""
@model
"""
type Store {
    """
    @OneToOne('User', 'username')
    """
    owner: String!
}
