"""
@model
"""
type User {
    """
    @unique
    """
    username: String!
    """
    @default('user')
    """
    role: Role!
}

enum Role {
    user
    admin
}

"""
@model
"""
type Test {
    """
    @OneToOne('User', '_id')
    """
    user: User!
}

"""
@model
"""
type Store {
    """
    @ManyToOne('User', 'username')
    """
    owner: String!
}
